import React, { Component } from 'react';

export default class ReactTextCollapse extends Component {
  static propTypes = {
    
  };

  constructor(props) {
    super(props);

    this.state = {
    }
  }

  render() {
    return (
      <div>
        hello React!
      </div>
    );
  }
}
