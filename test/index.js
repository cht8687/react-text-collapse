import test from 'tape';
import ReactTextCollapse from '../src/ReactTextCollapse';

test('ReactTextCollapse', t => {
  t.ok(ReactTextCollapse instanceof Function, 'should be function');
  t.end();
});
